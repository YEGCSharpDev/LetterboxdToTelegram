---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj",
  "LetterboxdToCinephilesChannel/Program.cs",
  "LetterboxdToCinephilesChannel/Worker.cs",
  "LetterboxdToCinephilesChannel/Configuration/TelegramOptions.cs",
  "LetterboxdToCinephilesChannel/Configuration/OmdbOptions.cs",
  "LetterboxdToCinephilesChannel/Configuration/RssOptions.cs"
]
autonomous: true
requirements: [NFR-2.1.1, NFR-2.3.2, NFR-2.2.2]

must_haves:
  truths:
    - "Application starts and maintains a background loop"
    - "Configuration fails fast if required environment variables are missing"
    - "Structured logs are emitted to the console"
  artifacts:
    - path: "LetterboxdToCinephilesChannel/Program.cs"
      provides: "Host configuration and dependency injection"
    - path: "LetterboxdToCinephilesChannel/Worker.cs"
      provides: "Main background execution loop"
    - path: "LetterboxdToCinephilesChannel/Configuration/TelegramOptions.cs"
      provides: "Validated configuration for Telegram"
  key_links:
    - from: "LetterboxdToCinephilesChannel/Program.cs"
      to: "LetterboxdToCinephilesChannel/Worker.cs"
      via: "AddHostedService<Worker>()"
      pattern: "AddHostedService<Worker>"
    - from: "LetterboxdToCinephilesChannel/Program.cs"
      to: "Configuration/TelegramOptions.cs"
      via: "Options Pattern with ValidateOnStart()"
      pattern: "ValidateOnStart"
---

<objective>
Initialize the .NET 10 Worker Service project structure and establish a robust configuration and logging foundation.

Purpose: Move from a simple console application to a modern, async-first background service that is ready for Docker deployment.
Output: A functional worker service that validates its configuration on startup.
</objective>

<execution_context>
@C:/Users/shnkr/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/shnkr/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase1/RESEARCH.md
@LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj
@LetterboxdToCinephilesChannel/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize .NET 10 Worker project structure</name>
  <files>
    LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj,
    LetterboxdToCinephilesChannel/Program.cs,
    LetterboxdToCinephilesChannel/Worker.cs
  </files>
  <action>
    1. Update `LetterboxdToCinephilesChannel.csproj`:
       - Set `TargetFramework` to `net10.0`.
       - Change `Sdk` to `Microsoft.NET.Sdk.Worker`.
       - Add NuGet packages: `Microsoft.Extensions.Hosting` (10.0.0), `Microsoft.Extensions.Options.DataAnnotations` (10.0.0).
    2. Rewrite `Program.cs` to use `Host.CreateApplicationBuilder(args)`:
       - Register `Worker` as a hosted service.
       - Use top-level statements.
    3. Create `Worker.cs`:
       - Inherit from `BackgroundService`.
       - Implement `ExecuteAsync` with a `while (!stoppingToken.IsCancellationRequested)` loop and `Task.Delay(TimeSpan.FromSeconds(10))`.
       - Add `ILogger<Worker>` injection and log a heartbeat message.
  </action>
  <verify>
    <automated>dotnet run --project LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj --property:RunCommand="dotnet" --property:RunArguments="build" && echo "Build successful"</automated>
  </verify>
  <done>The project builds and runs, outputting a heartbeat log every 10 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Options Pattern and Configuration Validation</name>
  <files>
    LetterboxdToCinephilesChannel/Configuration/TelegramOptions.cs,
    LetterboxdToCinephilesChannel/Configuration/OmdbOptions.cs,
    LetterboxdToCinephilesChannel/Configuration/RssOptions.cs,
    LetterboxdToCinephilesChannel/Program.cs
  </files>
  <action>
    1. Create `Configuration/` folder and option classes with DataAnnotations:
       - `TelegramOptions`: `BotToken` (Required), `ChannelId` (Required), `ApiId`, `ApiHash` (for MTProto).
       - `OmdbOptions`: `ApiKey` (Required), `BaseUrl`.
       - `RssOptions`: `FeedUrl` (Required), `PollIntervalMinutes`.
    2. Update `Program.cs` to bind and validate configuration:
       - Use `builder.Services.AddOptions<TOptions>().BindConfiguration("SectionName").ValidateDataAnnotations().ValidateOnStart()`.
       - Use environment variables for secrets (e.g., `TELEGRAM__BOTTOKEN`).
    3. Add a temporary check in `Worker.cs` to log the loaded configuration (masking secrets).
  </action>
  <verify>
    <automated>dotnet build LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj</automated>
  </verify>
  <done>Application fails to start if environment variables are missing, and logs validated settings when present.</done>
</task>

</tasks>

<verification>
- Run `dotnet run` with missing environment variables and confirm crash on startup.
- Run `dotnet run` with valid environment variables and confirm worker heartbeat logs.
</verification>

<success_criteria>
- Project upgraded to .NET 10 Worker SDK.
- Background loop operational.
- All core configuration sections bound, validated, and logged on start.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
