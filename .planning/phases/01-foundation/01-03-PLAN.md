---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [
  "LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj",
  "LetterboxdToCinephilesChannel/Infrastructure/Http/OmdbClient.cs",
  "LetterboxdToCinephilesChannel/Infrastructure/Http/RssClient.cs",
  "LetterboxdToCinephilesChannel/Infrastructure/Services/HistorySeedingService.cs",
  "LetterboxdToCinephilesChannel/Program.cs"
]
autonomous: true
requirements: [NFR-2.1.2, FR-1.2.3, FR-1.4.4]

must_haves:
  truths:
    - "External clients are injected with IHttpClientFactory"
    - "Standard resilience (retries, timeouts) is active for API calls"
    - "Database is seeded with existing movies from Telegram on first run"
  artifacts:
    - path: "LetterboxdToCinephilesChannel/Infrastructure/Http/OmdbClient.cs"
      provides: "Resilient OMDb Typed Client"
    - path: "LetterboxdToCinephilesChannel/Infrastructure/Services/HistorySeedingService.cs"
      provides: "Telegram history seeding logic using MTProto"
  key_links:
    - from: "LetterboxdToCinephilesChannel/Program.cs"
      to: "Infrastructure/Http/OmdbClient.cs"
      via: "AddHttpClient<OmdbClient>().AddStandardResilienceHandler()"
      pattern: "AddStandardResilienceHandler"
---

<objective>
Configure resilient Typed Clients for external integrations and implement the initial database seeding logic from Telegram history.

Purpose: Set up the communication layer for RSS and OMDb while ensuring the database starts with a complete record of already-posted movies.
Output: Resilient HTTP clients and a functional history seeding service using WTelegramClient.
</objective>

<execution_context>
@C:/Users/shnkr/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/shnkr/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase1/RESEARCH.md
@LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Typed Clients with Resilience</name>
  <files>
    LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj,
    LetterboxdToCinephilesChannel/Infrastructure/Http/OmdbClient.cs,
    LetterboxdToCinephilesChannel/Infrastructure/Http/RssClient.cs,
    LetterboxdToCinephilesChannel/Program.cs
  </files>
  <action>
    1. Add NuGet package: `Microsoft.Extensions.Http.Resilience` (10.0.0).
    2. Create `Infrastructure/Http/OmdbClient.cs`:
       - Inject `HttpClient`.
       - Implement a dummy `GetMovieById(string id)` method (for now).
    3. Create `Infrastructure/Http/RssClient.cs`:
       - Inject `HttpClient`.
       - Implement a dummy `GetFeed()` method (for now).
    4. Register clients in `Program.cs`:
       - Use `AddHttpClient<TClient>().AddStandardResilienceHandler()`.
       - Configure `BaseAddress` and default headers (User-Agent).
  </action>
  <verify>
    <automated>dotnet build LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj</automated>
  </verify>
  <done>Typed clients registered with standard resilience handler.</done>
</task>

<task type="auto">
  <name>Task 2: Implement History Seeding Logic (WTelegramClient)</name>
  <files>
    LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj,
    LetterboxdToCinephilesChannel/Infrastructure/Services/HistorySeedingService.cs,
    LetterboxdToCinephilesChannel/Program.cs
  </files>
  <action>
    1. Add NuGet package: `WTelegramClient` (4.1.0).
    2. Create `Infrastructure/Services/HistorySeedingService.cs`:
       - Use `WTelegramClient.Client` to connect (using `TelegramOptions:ApiId`, `ApiHash`).
       - Use `Messages_GetHistory` to fetch channel messages.
       - Parse message text for Letterboxd links/IDs.
       - Insert found movies into `ProcessedMovies` table if not already present.
    3. Update `Program.cs` to run seeding logic once at startup:
       - Check if `ProcessedMovies` is empty.
       - If empty, run `HistorySeedingService.SeedAsync()`.
  </action>
  <verify>
    <automated>dotnet build LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj</automated>
  </verify>
  <done>Seeding service implemented and integrated into startup sequence.</done>
</task>

</tasks>

<verification>
- Start application with a clean database.
- Observe logs for "Seeding history..." message.
- Verify `ProcessedMovies` table contains existing movies from the channel.
</verification>

<success_criteria>
- Typed Clients configured with Polly v8 resilience.
- WTelegramClient correctly fetches and parses channel history.
- Database seeded successfully on first run.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
