---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj",
  "LetterboxdToCinephilesChannel/Domain/Entities/ProcessedMovie.cs",
  "LetterboxdToCinephilesChannel/Infrastructure/Data/AppDbContext.cs",
  "LetterboxdToCinephilesChannel/Program.cs"
]
autonomous: true
requirements: [FR-1.4.1, FR-1.4.2, FR-1.4.3]

must_haves:
  truths:
    - "SQLite database file is created on startup"
    - "WAL mode is enabled for the SQLite connection"
    - "Migrations are applied automatically to the database"
  artifacts:
    - path: "LetterboxdToCinephilesChannel/Infrastructure/Data/AppDbContext.cs"
      provides: "EF Core database context"
    - path: "LetterboxdToCinephilesChannel/Domain/Entities/ProcessedMovie.cs"
      provides: "Persistence entity for processed movies"
  key_links:
    - from: "LetterboxdToCinephilesChannel/Program.cs"
      to: "LetterboxdToCinephilesChannel/Infrastructure/Data/AppDbContext.cs"
      via: "AddDbContext<AppDbContext>()"
      pattern: "AddDbContext"
---

<objective>
Implement EF Core with SQLite for portable persistence, ensuring the database is optimized for Docker and supports automatic migrations.

Purpose: Provide a reliable way to track processed movies and prevent duplicate Telegram posts.
Output: A SQLite database with a `ProcessedMovies` table and automatic migration logic.
</objective>

<execution_context>
@C:/Users/shnkr/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/shnkr/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase1/RESEARCH.md
@LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement EF Core SQLite Persistence and Entities</name>
  <files>
    LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj,
    LetterboxdToCinephilesChannel/Domain/Entities/ProcessedMovie.cs,
    LetterboxdToCinephilesChannel/Infrastructure/Data/AppDbContext.cs,
    LetterboxdToCinephilesChannel/Program.cs
  </files>
  <action>
    1. Add NuGet packages: `Microsoft.EntityFrameworkCore.Sqlite` (10.0.0), `Microsoft.EntityFrameworkCore.Design` (10.0.0).
    2. Create `Domain/Entities/ProcessedMovie.cs`:
       - `Id` (string, Primary Key - Letterboxd URL/ID).
       - `Title` (string).
       - `Year` (int?).
       - `ProcessedAt` (DateTime).
    3. Create `Infrastructure/Data/AppDbContext.cs`:
       - Define `DbSet<ProcessedMovie> ProcessedMovies`.
       - In `OnConfiguring`, use SQLite.
    4. Register `AppDbContext` in `Program.cs`:
       - Bind connection string from configuration (default to `Data Source=movies.db`).
  </action>
  <verify>
    <automated>dotnet build LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj</automated>
  </verify>
  <done>Entity and DbContext are implemented and registered in DI.</done>
</task>

<task type="auto">
  <name>Task 2: Configure WAL Mode and Automatic Migrations</name>
  <files>
    LetterboxdToCinephilesChannel/Infrastructure/Data/AppDbContext.cs,
    LetterboxdToCinephilesChannel/Program.cs
  </files>
  <action>
    1. Update `AppDbContext` or `Program.cs` startup logic:
       - Apply migrations on startup: `context.Database.Migrate()`.
       - Enable WAL mode: Execute `PRAGMA journal_mode=WAL;` on the connection after opening.
    2. Create the initial migration using `dotnet ef migrations add InitialCreate`.
  </action>
  <verify>
    <automated>dotnet ef --version && dotnet build LetterboxdToCinephilesChannel/LetterboxdToCinephilesChannel.csproj</automated>
  </verify>
  <done>Database is initialized with WAL mode and schema applied at application startup.</done>
</task>

</tasks>

<verification>
- Start application and check if `movies.db` is created in the output directory.
- Use a SQLite tool (or `sqlite3` CLI) to verify `journal_mode` is WAL: `PRAGMA journal_mode;`.
</verification>

<success_criteria>
- EF Core configured with SQLite.
- Automatic migrations running on start.
- WAL mode enabled for concurrent access safety.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
